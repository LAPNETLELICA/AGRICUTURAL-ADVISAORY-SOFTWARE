# Regional knowledge (T3)

Developer 2 owns regional fit, local agronomic constraints, and location-specific
rules here. JSON files must follow `../rules/schemas/rule.schema.json` and use
`domain: "T3"`.
